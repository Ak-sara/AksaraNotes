<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Biometric Authentication (Android 12+) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- File Access (for backup/restore) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Android 13+ Granular Media Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AksaraNotes">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ADD THIS: Setup Activity -->
        <activity
            android:name=".ui.auth.SetupActivity"
            android:exported="false"
            android:theme="@style/Theme.AksaraNotes" />

        <activity
            android:name=".ui.notes.NoteDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.AksaraNotes"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.database.builder.TableBuilderActivity"
            android:exported="false"
            android:theme="@style/Theme.AksaraNotes"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.database.view.TableViewActivity"
            android:exported="false"
            android:theme="@style/Theme.AksaraNotes"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.database.items.ItemEditorActivity"
            android:exported="false"
            android:theme="@style/Theme.AksaraNotes"
            android:parentActivityName=".ui.database.view.TableViewActivity" />
    </application>

</manifest>